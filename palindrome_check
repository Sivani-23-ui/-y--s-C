#include <stdio.h>
int main() {
    int n, rev = 0, temp;
    scanf("%d", &n);
    temp = n;
    while(temp) {
        rev = rev * 10 + temp % 10;
        temp /= 10;
    }
    printf(n == rev ? "Palindrome\n" : "Not Palindrome\n");
    return 0;
}
